- [x] Ensure 3D landmark buildings occlude other layers properly (i.e., rideshare vehicles shouldn't be visible when driving behind a building, from the user's perspective) — landmark massing raised to z=2.8 (above agents/cameras); `landmarks_occlude_agents` test guards the ordering.
- [x] Toggle off the Woolworth Building, and the Chrysler Building, and *add* the Met Cloisters in Inwood, and the Apollo Theater in Harlem. — landmark set re-extracted from CityGML (Apollo BIN 1058654, Cloisters BIN 1064383; Woolworth+Chrysler dropped); asset re-baked + copied to web/dist.
- [x] Add layer-specific appearences for the highlighted state in the 'my area' mode. — in-shed cameras now get a layer-colored ring (CCTV gray / DOT steel / ALPR yellow / enforcement orange) via new `WalkshedSummary.camera_kinds`; `walkshed_reports_camera_kinds_aligned_with_points` test added.
- [x] Improve the text scaling for the DOT, CCTV, and FLOCK layers -- they are too small unless users zoom very far into the map. — `scale_camera_icons` floors icons to a min on-screen size (`MIN_ICON_HALF_PX`) by rebuilding the merged-quad meshes on zoom; `camera_icons_floor_to_min_screen_size` test added.
- [x] Make the 'Google Maps' route input UX bidirectional -- if a user clicks on the map, it should find the nearest address to that coordinate they clicked and update the address / start / end boxes as relevant. — map click now reverse-geocodes (GeoSearch `/reverse`) into the Start/Dest/area box via `Geocoder::reverse_lookup`; `reverse_result_resolves_field_without_dropdown` + `reverse_with_no_match_flags_the_box` tests added; README privacy note updated (a click sends that one coordinate).
- [x] Make the ACE bus routes more noticeable, especially when zoomed out. — ACE corridors now thick quad ribbons (`world::thick_line_list_mesh`) with a zoom-floored on-screen width (`scale_ace_corridors`, `MIN_ACE_HALF_PX`); `ribbon_offsets_perpendicular_by_half_width` + `ace_ribbons_floor_to_min_screen_width` tests added.
- [x] Add LinkNYC kiosks to the map as another fixed layer, but note that the surveillance comes when you connect to their wifi. https://data.cityofnewyork.us/City-Government/LinkNYC-Kiosk-Status/n6c5-95xh — `LinkNycLayer` (1,225 Manhattan kiosks, `bake-linknyc`), cyan square markers (`LinkNycVis`, `linknyc_on` toggle, off by default, zoom-floored via `scale_markers`); the "surveils when you connect to Wi-Fi" caveat is the toggle hover note; `bakes_kiosks_skips_missing_coords_and_flags_live` test added.
- [x] Add Open Parking and Camera Violations as a *surveillance evidence* layer, narratively and via UX intertwining it with the ACE bus routes layer. https://data.cityofnewyork.us/City-Government/Open-Parking-and-Camera-Violations/nc67-uf89/about_data — dataset has no coordinates, so it surfaces as a "⚖ Surveillance evidence" callout under the ACE toggle (242,742 Manhattan bus-lane violations / $27.9M fines, from nc67-uf89; snapshot in data/snapshots/violations/); `group_thousands`/`compact_usd` helpers + `evidence_number_formatting` test.
- [x] Toggle off smart glasses by default, and put them and the sidewalk delivery robots behind a new 'In 5 years...' button on the right sidebar. — `glasses_on` now defaults off; new "🔮 In 5 years…" sidebar button flips both speculative layers via `Params::set_future(future_on→glasses_on+robots_on)`; Advanced rig keeps the density sliders gated on it; `future_mode_groups_speculative_layers` test added.
- [x] Remove layers that are not currently visible from the stacked sensors by operator view, dynamically. — `operators_layout` now lays out only `present_columns(&counts)` (count > 0), so toggled-off layers drop their lane + header dynamically; `present_columns_drops_empty_layers` test added.
- [x] Tweak the Neighborhood Camera Density heatmap to not be as occluded by the building footprint layer, and to more clearly show relative differences. Think perceptually uniform colormaps, etc. — `HEAT_COLORS` swapped to a perceptually-uniform inferno ramp (light→dark, density = darkness); alpha floor raised 0.20→0.42 so low cells stay visible + dense ones sit solidly (footprints/landmarks already hidden in heatmap mode); `heatmap_colormap_is_perceptually_ordered` test added.
- [x] Add a 'StoryMap' mode that plays a 'video' of sorts -- essentially a predefined set of clicks & UX interactions that we can link to to educate users on specific scenarios & components of the application. The first StoryMap should be a 'Tutorial' of sorts, walking the user through the application. — `storymap.rs` engine (steps + timing, unit-tested) + `storymap_tick` (applies each scene) + `storymap_ui` (launcher + caption bar w/ transport); 8-step Tutorial; clicks inert during play; deep-linkable via `?story=tutorial` (web-sys); 4 engine tests.
- [x] Add another StoryMap that tells a longitudinal story, going from 10 years ago to now, to 5 years in the future (utilize the In 5 years... mode) — `longitudinal()` story via a new composable `StepAction::Scene`; eras differ honestly (LinkNYC kiosks off in 2015 → on now, heatmap/operators show today, finale flips `set_future`); "🕰 10 years" launcher + `?story=longitudinal` deep-link; `longitudinal_runs_then_to_now_to_future` test.
- [x] Replace the 'FLOCK' label with 'ALPR' — `OperatorCol::Flock.label()` now "ALPR" (operator column header), neighborhood breakdown label "ALPR", gloss keeps "Flock + agency plate readers"; `alpr_column_labeled_alpr_not_flock` test added.
- [x] Remove the Columbia Low Library 3D building, as well as the UN Secretariat Building. — landmark set re-extracted without Columbia + UN (now 8: One WTC, Flatiron, Empire State, Grand Central, 30 Rock, Met, Apollo, Cloisters); asset re-baked + copied to web/dist.
- [x] Add contextual on-click modal (attached to a ALPR point) when user clicks on an ALPR, mapping to the deflock metadata for the camera, i.e., https://maps.deflock.org/?lat=40.7391&lng=-73.9829&zoom=17.60, https://www.openstreetmap.org/node/13837758401#map=19/40.738822/-73.983360 — clicking within ~16px of an ALPR opens an egui modal (`ui::alpr_modal`) with the maker (red headline) + operator + heading + deep-links to OpenStreetMap (`/node/{osm_id}`) and DeFlock (`?lat&lng`). `handle_click` hit-tests the new `AlprDirectory` resource (Manhattan readers w/ metadata + lat/lon) before placing a route/area point. Compiles + boots clean; interactive click couldn't be verified headless (the inspector can't deliver mouse events to winit — real-browser clicks work).
- [x] Add the above ALPR metadata actually into the ALPR layer, so we can stratify by the maker of each camera -- ie., Mav Systems, Redspeed, Ekin, Flock. Useful for stack by operator view. — DATA DONE earlier (`bake-alpr` captures `manufacturer` 435/444 + `operator`). NOW STRATIFIED: new `operators::Maker` classifier (Flock/Leonardo·ELSAG/Redspeed/Ekin/Mav/Genetec/Motorola·Vigilant/Other, case-insensitive) + categorical palette. The ALPR tower sorts chips by maker (contiguous bands) and tints each via per-vertex `ATTRIBUTE_COLOR` written in only when stacked (`operators_chip_material`), so map owls stay uniform red. The Stack-by-operator panel lists the maker breakdown (count + color swatch) under ALPR (`AlprMakerBreakdown` resource). Tests: `maker_classify_maps_brands_and_aliases`, `maker_colors_are_distinct`. Compiles + boots; tower banding not headless-verifiable (real-browser only).
- [x] Ensure all ALPRs on deflock that have field-of-view are rendered as such in the our-space application. — the bake only parsed single-numeric `direction` (130/444 → a wedge); the 314 multi-reader gantries carry semicolon-separated bearings (`"28;209"`) that failed to parse and fell back to omnidirectional. `alpr::bake` now takes the primary bearing, so **all 444 directional ALPRs** get a heading → render a 70° FOV wedge (when FOV is shown) instead of an omnidirectional dot.
- [x] Add similar modal for CCTV layer, indexing metadata from the actual Dahir et al dataset — new `CctvCameraLayer` (own `.oscctv` ext + `CctvRes`, so enforcement keeps plain `.oscam`/`FixedSensorLayer`) carries per-camera provenance: `CctvSource::{Amnesty,Dahir}` + (for Dahir) the GSV `panoid`, capture year/month, heading. `bake-cctv` re-baked (4266 Amnesty + 156 Dahir-unique). `ui::cctv_modal` shows the source + a Google Street View deep-link (exact Dahir panorama via `panoid`, else by coords); `handle_click` hit-tests `CctvDirectory` after ALPR. batch/route_demo updated to the new type. Compiles + boots clean (4312 CCTV loaded); interactive click not headless-verifiable (real-browser clicks work).
- [x] Make dashcam vehicles and buses obey speed limits -- construct piecewise routes from origin-destination that obey speed limit instead of one linear constant-velocity traversal. — Buses already follow real per-stop GTFS time→arc keyframes (dwell + eased travel). Taxis/dashcam vehicles traversed at one constant speed (`frac * total_m`); now they replay through a new `sim_core::PaceProfile` (turn-aware, speed-limit-capped): brake into corners (down to 30% of limit), cruise on straights at ≤ NYC's 25 mph default (`NYC_SPEED_LIMIT_MPS`), same total trip duration. Computed once per pooled route (`Sim.taxi_paces`), applied in `replay_agents`. EdgeData carries no `maxspeed`, so this is a runtime pace (no re-bake); honest about the cap (instantaneous speed stays ≤ limit whenever a trip's average is, i.e. nearly always in traffic). Tests: `pace_profile_endpoints_and_monotonic`, `pace_profile_slows_through_turns`, `pace_profile_degenerate_is_linear`. Motion is render-verifiable (vehicles visibly slow at turns).
- [x] Tune the shadowing/shading on the landmarks -- right now, shadows for the empire state building clip through the building itself, etc. — the per-face painter sort keyed on the **height-extruded** screen-y centroid, so a tall *near* (south) wall sorted as if far back and dark far faces drew over it (the "shadow clipping through" artifact). Fixed to sort on the **ground-plane** north–south position only (`s.verts[i][1]`, un-extruded), the true occlusion axis for the top-down-oblique view; roofs still drawn last. Render-verifiable.
- [x] Add a visible isochrone to the 'my area' functionality when an address is selected that encompasses the study area. — `place_walkshed` now draws the convex hull of every reachable node as a translucent gold wash (`filled_polygon_mesh`) + a deeper-amber boundary (`stroke_ring_mesh`) beneath the gold streets, so the 10-minute study area reads as one shape. New `convex_hull` helper (monotone chain).
- [x] Add a 'random location' button to my area mode, and a 'random route' button to the walk mode. — "🎲 surprise me" (My area) drops the walkshed on a random graph node; "🎲 random walk" (A→B) routes between two well-separated random nodes (700–2500 m). Flags on `Geocoder` (`random_walkshed`/`random_route`), consumed in `apply_geocode` with a persistent `WyRand` (new pick each press), reverse-geocodes the picks into the address boxes + flies the camera.
- [x] Remove the 'our-space' duplicate header from the top of the right sidebar. — the page's HTML `<header>` already carries the "our·space" wordmark, so the panel masthead dropped its duplicate title and now leads with the question ("How watched is a place in Manhattan?").
- [x] More cleanly and readably format the 'About the data & its limits' dropdown content on the right sidebar. — regrouped from a flat label wall into sections (WHERE THE DATA COMES FROM with bold source name + wrapped description per source, HOW SOURCES ARE COMBINED, WHAT'S REAL/MODELED) for scannability.
- [x] Add a 'more reading' panel that runs to the right of the web-deployed-version header (our dot space - survey of the watched commons area). New reading cards should be add-able via a new markdown file in a content management folder. Let's start by adding the papers https://dl.acm.org/doi/full/10.1145/3715275.3732185 and https://dl.acm.org/doi/10.1145/3593013.3594020, and the article https://www.malwarebytes.com/blog/news/2025/09/nexar-dashcam-video-database-hacked. — header restructured into a `.masthead` flex row: the wordmark/subtitle `.lead` on the left, a `.reading` "Further reading" rail on the right (left-border separator, wraps below on < 760px). It's a tiny CMS: each card is one markdown file in `web/content/reading/` (front-matter title/source/kind/url/order + a blurb), baked to `web/dist/reading.json` by `tools/build_reading.py` (wired into `web/build.sh`, skips `README.md` + files missing title/url), fetched + rendered (XSS-escaped, 4-line blurb clamp, PAPER/ARTICLE badge) at runtime — fails silent (panel hidden) with no `reading.json` (e.g. file://). Seeded with the two FAccT papers (titles/authors confirmed via Crossref) + the Malwarebytes Nexar-breach article; `web/content/reading/README.md` documents the drop-a-file workflow. Verified in real Chrome via `tools/inspect` (panel renders top-right in the journal aesthetic, full blurbs, links open in a new tab).
- [x] Convert the 'neigborhood camera density' checkbox into its own mode, on the level of 'my area' and the walk mode; when neighborhoods is clicked, the other two modes should not be interact-able. — new `Mode::Neighborhoods` joins `Walkshed`/`Route` as a third selector button ("My area · A walk A→B · Neighborhoods"); `ui_panel` derives `params.neighborhoods_on = matches!(mode, Neighborhoods)` each frame so the choropleth systems follow the selector, and the My-area/walk address inputs + result card are swapped out for the all-boroughs toggle + the hovered-region breakdown (`result_neighborhoods`, moved out of the old EXPLORE checkbox). `sync_mode` already clears area/walk visuals on switch; `handle_click` early-returns in neighborhood mode (clicks inert — the breakdown is hover-driven, markers hidden). The direct `neighborhoods_on` setters in `shot_capture`/`smoke_exit` now set the mode instead. Compiles + native shot verified (3-button selector fits, "Neighborhoods" selected, breakdown card prompts on hover); 33 tests pass. Interactive mode-switch/hover is real-browser-verifiable.
- [x] Devise a plan for scaling the visualization to all five boroughs of NYC, including pre-emptive performance monitoring & profiling. — written up as `docs/SCALING.md`, grounded in the current Manhattan baseline (6,762 graph nodes, ~4,590 camera nodes, 38 MB bundle, the 45 m exposure grid clamped to ~100 k cells, the `MAX_*` agent caps). Key insight: several costs are already *extent-bounded* (the grid clamp coarsens; agents are capped + subsampled) and degrade gracefully, while the real work is *data-volume* — footprints (4.8 MB → ~50 MB), the street graph (~10–15×), the TLC trip tables, and the citywide camera census. Names the ranked bottlenecks (download weight is the gating one: a naive re-bake → 300–500 MB), then a phased path: **Phase 0 instrument first** (a `?debug=perf` HUD via `FrameTimeDiagnosticsPlugin`, a `tools/inspect --profile` frame-time matrix over {borough × zoom × layers}, a bundle-size budget gate in `build.sh`, and a committed `docs/perf-baseline.json`), then partition the pipeline by borough (it already takes a clip arg), load on demand + LOD, spatial indices + viewport-aware spawning, bounded routing, and ship borough-by-borough against held budgets (p95 ≤ 16.6 ms, interaction < 150 ms). No code yet — it's the requested plan.
- [x] The CCTV/Flock on-click modals should only trigger when the user is zoomed in far enough -- right now, the 'my area' functionality is often sabotaged by an on-click modal at far zoom levels. — `handle_click` now gates the ALPR + CCTV marker hit-tests behind `cam_t.scale().x <= MODAL_ZOOM_MAX_MPP` (4.0 m/px); zoomed out past that, a click skips the modals and falls straight through to area/route placement (markers are tiny + dense there anyway). Compiles; interactive verify in-browser.
- [x] Enure the live walk collision/detection logic is working correctly -- right now, dashcam vehicles only add to the total cameras encountered count about 1/3 of the time. — root cause was two-fold. (1) The ~1/3 was an *intentional* Bernoulli: dashcams counted at `p = penetration 0.40 × capture_prob 0.40 = 0.16` to match the analytical headline. Per the chosen calibration ("count every pass, deterministic"), dashcam vehicles now register **every** in-range pass (`p_vehicle = 1.0`) + flash, so the live walk shows each device that physically drives by (decoupled from the headline estimate, which still reports the calibrated expectation); the other mobile classes keep their capture-given-pass roll. (2) A genuine correctness gap: at higher clock rates agents move many metres/frame and could **tunnel** past the capture radius between sampled frames. `mobile_capture_events` now does a swept closest-approach test — `point_segment_distance(walker, prev_pos, cur)` over each agent's per-frame segment (new `MobileAgent.prev_pos`), with a `MAX_SWEEP_M = 600 m` clamp so a slot-reassignment teleport degrades to a point test instead of sweeping across the map. New `swept_pass_beats_point_test_no_tunneling` test pins the anti-tunneling property; 33 app tests + sim-core math tests pass, native boot + WASM build clean. Per-pass counting is real-browser-verifiable (live walk).
- [x] Add https://www.pahouse.com/InTheNews/NewsRelease/?id=143905 to further reading. — added `web/content/reading/04-smart-glasses-recording-indicator.md` (kind `policy`): Rep. Joe Ciresi's H.B. 2603 requiring smart glasses to show a recording indicator (PA House, June 2026), tied to the "In 5 years…" glasses layer. Picked up automatically by the CMS generator (now 4 cards).
- [x] Make the further reading panel one column instead of one row, and have it horizontally span the whitespace that runs along the web-deployed header, to the right of the application tag/brief description. — confirmed the intent via AskUserQuestion ("one wide column, right side"): `.reading` changed from a fixed ~340px right rail to `flex: 1 1 auto; max-width: 680px; margin-left: auto`, so it's a single wide column of cards filling the header whitespace to the right of the wordmark (header grows to fit; wraps below the wordmark < 760px). Verified in real Chrome via `tools/inspect`.
- [x] Remove the brief description from the site tag section of the header (at top left.) — dropped the `.subtitle` paragraph (and its now-unused CSS) from the header.
- [x] Turn the all-lowercase our.space logo to all-caps. — wordmark is now `OUR·SPACE` (terracotta `·` kept).
- [x] Remove the 'survey of the watched commons - manhattan' element. — dropped the `.seal` badge (and its now-unused CSS) from the header.
- [x] Add the current simulation day (April 21, 2026, I believe) in large font at top-left of the map container. — a non-interactive egui `Area` anchored LEFT_TOP over the map canvas shows a tiny terracotta "SIMULATED DAY" eyebrow + `SimDate.label` ("Tuesday, April 21, 2026") large in the display font, on a faint paper plate so it reads over the choropleth. Verified in the native `OURSPACE_SHOT` map capture.
- [x] Slow the default simulation down to 40mim/day, from 18min/day. — `DEFAULT_SIM_RATE` 90 → 36 sim-sec/real-sec (`86400/(36·60)` = exactly 40 min/day); still within the `[SIM_RATE_MIN 15, SIM_RATE_MAX 1440]` speed-control range.
- [x] Ensure 'FLOCK' layer of ALPRs is actually labeled as 'ALPR' on the map. — already "ALPR" in the current source: the on-map Operators tower header renders `OperatorCol::Flock.label()` (= "ALPR", from item 14) via `operators_headers`. Verified in a fresh `OURSPACE_SHOT` operators capture (column reads "ALPR · 284" with the maker bands). The lingering "Flock" references are legitimate — the brand in the gloss ("ALPR — Flock + agency plate readers") and the Flock *maker* in the breakdown. The stale "FLOCK" the user saw was a pre-rebuild deploy; this build makes it definitive.
- [x] Add https://www.404media.co/metas-ray-ban-glasses-users-film-and-harass-massage-parlor-workers/ to further reading. — added `web/content/reading/05-rayban-glasses-harassment.md` (kind `article`): 404 Media's report (Maiberg, Oct 2025) on Ray-Ban-glasses users covertly filming massage-parlor workers — the glasses layer's harm already realized. CMS now bakes 5 cards.
- [x] Add the adobe fonts webproject <link rel="stylesheet" href="https://use.typekit.net/vfq0lcs.css"> to this site -- and replace all fonts with the Host Grotesk font set -- use differing weights and stylings as appropriate to create a clean and polished look. This should look like a serious journalistic endeavour, like something the NY Times' Data Visualization Team would put out. — unified the whole app on **Host Grotesk**, one family with hierarchy from weight/case/tracking (the NYT data-viz approach). Web shell (`web/index.html`): swapped the Google serif stack (Fraunces/Spectral/IBM Plex Mono) for the Adobe Fonts kit `use.typekit.net/vfq0lcs.css`; `--display`/`--body`/`--mono` all → `"host-grotesk"`; wordmark to 800 / -0.025em (dropped the serif emboss), labels 700 + wider tracking, card/overlay titles 700. In-canvas egui (`theme.rs`): baked static `HostGrotesk-Regular` (400) + `HostGrotesk-ExtraBold` (800) from the OFL variable font (fonttools instancer) into `assets/fonts/`, `install_fonts` loads them in place of Archivo. Verified in real Chrome — `document.fonts.check` confirms the kit resolves on-origin (host-grotesk 400 + 800), both surfaces render in the new face. 
- [x] Add 3D landmarks for the NYC bridges (decks + towers + cables). — `tools/generate_bridges.py` traces deck ribbons from named CSCL bridge centerlines (clipped to a PCA main-span window), places portal towers (legs + crossbeam) from a curated height/span table, and drapes parabolic main cables + back-spans — all emitted as landmark-schema JSON so `bake-landmarks` + `world::landmark_massing_mesh` render it with zero new Rust geometry. 9 iconic spans (Brooklyn/Manhattan/Williamsburg/Queensboro/GW/Verrazzano/Throgs Neck/Whitestone/RFK). New `BRIDGES_PATH`/`LoadingHandles.bridges` (reuses `LandmarkRes`+`.oslmk` loader) loaded in BOTH builds; `BridgeVis` render block at `LANDMARK_MASSING_Z`; visibility folded into the landmark query (`Or<(With<LandmarkVis>,With<BridgeVis>)>`, follows `landmarks_on`). Baked into `tools/bake_citywide.sh` (named-segment CSCL pull persisted at `data/snapshots/osm/cscl_bridges.geojson`). Verified native (3 East River spans read clearly with towers above decks); WASM built + asset ships to web/dist (200). **Follow-up fix (Queensboro + RFK):** Queensboro's cantilever was rendering as two solid full-height piers (fat blocks) → now a proper double-cantilever truss — portal towers + a `cantilever_profile` truss web that humps over the piers, dips to the suspended mid-span, tapers to the approaches, and carries *no* cables (correct for a cantilever). RFK is three crossings meeting at Randall's/Ward's Island; PCA-over-everything smeared one straight suspension span diagonally across the island → now an explicit `anchor` (two lon/lat tower points) pins the iconic East River (Hell Gate) span on the Astoria↔Ward's Island water crossing (1,380 ft span, 335 ft towers, Ammann 1936) with a synthetic straight deck. Other 7 suspension spans unchanged (deck emission refactored into a shared `deck_ribbon` helper, behavior-preserving). Re-verified native (RFK = clean span on the water; Queensboro = trussed deck + towers, no cables); 35 app tests pass; asset re-baked + synced to web/dist. **Approaches fix:** the deck was clipped to a PCA main-span *window* (±~340 m), so long bridges floated mid-river with gaps to both shores (glaring on Queensboro over the wide East River). Found the named-CSCL bridge centerline is ramp-free (lanes spread <35 m perp), so the window was purely harmful — replaced it with a wide perpendicular *corridor* clip (±120 m) that traces the FULL centerline shore-to-shore (Queensboro now spans the whole 1.9 km Manhattan→Roosevelt I.→Queens crossing and meets the street grid on both ends; Brooklyn/Manhattan/Williamsburg also reach their abutments) while still dropping any stray off-axis spur. The cantilever truss web now spans the real decked extent (`t_lo..t_hi`) with `cantilever_profile` tapering to the true deck ends.
- [x] Add API-based citywide real taxi trips to the full-nyc build of the application. — done via **path A** (user-chosen): real citywide trips, baked (HVFHV per-trip is Parquet-on-cloudfront, not a Socrata API, so a live per-trip fetch isn't possible; the data step is one DuckDB extraction). Reconstructed the previously-undocumented DuckDB query and extracted one real Tuesday (2024-06-25) of all-borough HVFHV **without** the Manhattan zone filter → **547,263 trips, 258/259 PU/DO zones** (`taxi_trips_all_nyc.csv` + a locally-derived `taxi_perminute_od_nyc.csv`). `bake-taxi-day` over **`graph_nyc.osgraph`** → `taxi_day_nyc.ostaxiday` (**391,380 trips on 5000 routes, 0 no-path**, 8.0 MB). Wired in: `TAXI_DAY_PATH_NYC` swapped into the citywide asset tuple + `dashcam_on=true` in the citywide block (the `dashcam`/Vehicle gate, `agents.rs:713`). Verified native: rideshare clay-triangles now drive the Brooklyn/Williamsburg street grid at 14:30 (none before). The full pipeline (DuckDB extraction + bake) is now reproducible in `tools/bake_citywide.sh`, filling the doc gap noted in docs/TAXI_GENERATION.md (also updated: citywide is now ON). The *analytic* dashcam exposure field (`DashcamFieldLayer`) turned out to **already be citywide** (it's baked from the all-borough `zone_trips.csv` over all 263 taxi zones, no Manhattan filter — `intensity_at` returns real per-borough density: Manhattan 3.74× / Brooklyn 1.94× / Queens 0.51× median), so the exposure model already samples it everywhere; no follow-on needed. Compiles + 102 tests pass.
- [x] Document thoroughly how taxi trips are currently generated. What is the granularity of origin/destination, route heterogeneity for the same origin/destination, and similar details. — wrote `docs/TAXI_GENERATION.md` (TL;DR + 6 sections + file:line map). Key findings: on-screen taxis replay one real day of **TLC High-Volume FHV (Uber/Lyft)** records (Manhattan↔Manhattan, baked by `taxi_day.rs` into `TaxiDayLayer`; the older `vehicle_routes.rs` pool is vestigial). **O/D granularity = taxi zone, not coordinates** — HVFHV carries only PU/DO LocationID (~69 Manhattan zones), each → one area-weighted centroid → one nearest street node, so every trip from a zone shares an identical start node. **Route heterogeneity = none** — each (PU,DO) pair maps to exactly one A* route_idx; all trips on a pair share one polyline (variety only from ≤5000 distinct O/D pairs + per-trip minute/duration + the turn-aware 25 mph `PaceProfile`). Routes run on the pedestrian walk graph (one-way/turns ignored) → decorative, excluded from the headline estimate. Measured 164,184 trips / peak 3,823 concurrent; `MAX_VEHICLES=4000`. Taxis OFF in `?city=nyc` (Manhattan-only routes) — exactly the gap item #41 fills.
- [x] Tweak and polish the 'stack cameras by operator' transition animation -- right now, the layers turn into squares/primary shapes as soon as we click the button; ideally, they maintain their original appearance until they are 'sucked' off the map, and then midway through the transition, they can turn into the shapes used in the by-type bar graphs. — `operators_chip_material` + `operators_mobile_material` were **edge-triggered on `ov.active`**, so they stripped the icon texture → solid chip the instant the view turned on (squares-on-click). Now both drive the texture/color swap off animation *progress*: a sensor keeps its on-map mark (branded wordmark / moving bus·glasses icon) while `ov.t < CHIP_MORPH_T` (0.5) and only becomes the solid bar-graph chip at the midpoint — so the marks fly off the map looking like themselves, then snap to the chart shape mid-flight. Keyed the per-frame early-return on the derived `solid` boolean (not `ov.active`) so the crossing is detected in both directions; ALPR maker-tint write moved onto the same `solid` gate. Verified native: a mid-flight capture shows CCTV/DOT/ENF wordmarks + bus icons still textured during liftoff (old code = squares), and the settled tower still renders solid chips with maker bands. 35 app tests pass.
- [x] Ensure the reverse geocoding in the routing UX works citywide, not just in Manhattan. Right now, clicking on a place in south brooklyn yields an incorrect location. — root cause: `geocode::parse_geosearch` dropped every candidate outside a hardcoded **Manhattan bbox**, so a Brooklyn click lost its true (Brooklyn) nearest match and `resolve_reverse` fell back to whatever stray in-box Manhattan candidate remained — the "incorrect location." Fix: a scope-aware accept-box — added a five-borough `NYC_LAT/LON` box (matches the citywide graph extent) selected by a `set_citywide()` flag (a module `AtomicBool`, set from `CityScope` at startup). The click→lat/lon path already used scale-independent `to_wgs84`, so it was correct; only the filter was wrong. Refactored the check into a pure `in_box(lat,lon,citywide)` and added `accept_box_widens_to_all_boroughs_when_citywide` (Coney Island + Flushing rejected in Manhattan build, accepted citywide; Manhattan in both; deep-NJ out of both). 34 app tests pass.
- [x] Add Parks to the map, from here: https://data.cityofnewyork.us/Recreation/Parks-Properties/enfh-gkve. Color them green. — new `data-pipeline bake-parks` (`parks.rs`, reuses the footprint RDP+area helpers + `BuildingFootprints` payload under a `.ospark`/`ParksRes` loader) bakes the Parks Properties polygons (geometry col `multipolygon`) into flat ENU rings, RDP @2.5m, ≥400 m², borough-filterable. Baked twice: Manhattan-clipped (`parks.ospark`, 531 polys/37KB) for the default build, all-five-borough (`parks_nyc.ospark`, 3305/258KB) for `?city=nyc`. App renders one merged green mesh (`merged_footprints_mesh`, muted sage `ca(0x86,0xa8,0x66,0.92)`) at z −0.16 (under the footprints so a park's interior buildings still read, above the choropleth); `ParksVis` + `parks_on` toggle (default on, in "More layers"), hidden under heatmap/choropleth like the footprints. Verified native (Central Park + pocket parks render green); wired into `bake_citywide.sh`.
- [x] Add pedestrian plazas to the map, from here: https://data.cityofnewyork.us/Transportation/NYC-DOT-Pedestrian-Plazas-Polygon/k5k6-6jex/about_data. Texture them with hatched concrete/tiling etc. — new `bake-plazas` (`plazas.rs`, geometry col `the_geom`) bakes the 93 plazas → 152 polys (`plazas.osplaza`, 12KB, one asset both builds). App renders a warm-concrete fill (`ca(0xd7,0xd1,0xc6,0.95)`) at z −0.08 (above the footprints, below the streets) + a 45° **hatch clipped to each polygon** via new `world::hatch_lines_mesh` (even–odd scanline fill, thin warm-gray ribbons) as the paving texture; `PlazasVis` + `plazas_on` toggle. Verified native (Municipal Plaza renders as a diagonally-hatched concrete slab beside City Hall Park); wired into `bake_citywide.sh`.
- [x] Add pedestrian plazas to the map, from here: https://data.cityofnewyork.us/Transportation/NYC-DOT-Pedestrian-Plazas-Polygon/k5k6-6jex/about_data. Texture them with hatched concrete/tiling etc. — duplicate of the item above; already shipped (`bake-plazas` → `plazas.osplaza` in both builds, concrete fill + clipped 45° hatch via `world::hatch_lines_mesh`, `PlazasVis`/`plazas_on` toggle). Re-validated: `hatch_fills_a_square_and_skips_degenerate` passes, asset present in `web/dist` (12,762 B), render block at `main.rs:1469`.
- [x] Ensure the testing suite is up to date, encompassing of all functionality, and comprehensive -- it should catch regressions that might occur as we add new features. — audited coverage and filled the gaps for every feature added this session: `geocode::accept_box_widens_to_all_boroughs_when_citywide` (the citywide reverse-geocode fix), `world::hatch_fills_a_square_and_skips_degenerate` (the new plaza hatch-clip geometry), `parks::borough_code_maps_names_codes_and_unknown` (the parks borough filter). Bridges reuse the already-tested `landmark_massing_builds_mesh` renderer. Whole-workspace suite green: **app-interactive 35, data-pipeline 10, sim-core 57 (102 total), 0 failed.** (Comprehensiveness is an ongoing concern — bake I/O paths remain integration-tested-by-running, like the pre-existing footprints bake — but every new pure-logic path now has a regression guard.)
- [x] Replace the two 'further reading' paper placeholder images with 'dsi.png' and 'policing.png' in content/reading/images. Optimize the images for web first. — cover-cropped + stripped both to 480×270 teasers via ImageMagick (`dsi.png` 771KB → `01-privacy-of-groups.jpg` 34KB for the Dense-Street-Imagery privacy paper; `policing.png` 3.5MB → `02-police-deployment-dashcams.jpg` 28KB for the dashcam-policing paper, q82). Named after the card stems so `find_image` auto-resolves them, and `.jpg` outranks the old `.svg` placeholders in `IMG_EXTS`. `build_reading.py` confirms both cards now point at the new teasers (all 5 cards have photo teasers). The original drop-offs are left in place (unused — they match no card stem, so never ship).
- [x] Trim the rest of the layers in the manhattan-only route of the site -- no cameras, sensors, parks, plazas, or anything should be rendered outside of manhattan. — the leaks were plazas (one citywide asset), bridges (all 9 NYC spans), **and the fixed-camera layers themselves** (the data is NOT Manhattan-only — DeFlock ALPRs especially carry outer-borough cameras, which is what was still showing up). `build_world` now filters plazas + bridges to Manhattan's main landmass (`boro.0.rings[0]`, point-in-ring on any vertex) when `!citywide`, and — the camera fix — the `man` clip region applied to every fixed sensor (CCTV/ALPR/DOT/enforcement via the `keep` closure, which also gates the per-source map markers built from `sensors`) is now `boro.0.rings[..1]` (Manhattan only) in the default build vs all five boroughs' rings in the citywide build. That drops the Manhattan build to 5,447 sensors, all on-island. Parks are borough-clipped (#52). The faint other-borough coastline outlines are kept as the geographic frame (the line #53 keeps off bridges); only data layers are clipped. Verified native (wide + Brooklyn-pan shots: every camera/park on Manhattan, the East River + outer boroughs empty).
- [x] Trim parks properties layer to not exceed the extent of the borough boundaries lines -- for example, Randall/Ward's island has a parks property extent that extends out in the water presently, which does not make sense. — `parks::bake` now takes the shoreline-clipped DCP borough boundaries (`borough_boundaries.geojson`, the same source as the coastline outline) as a clip mask and intersects every park polygon against the land via `geo::BooleanOps` before projection, so a park straddling the bank is trimmed and one entirely in water is dropped (citywide: 4 dropped, hundreds trimmed). Perf: land simplified to ~5 m + a per-borough bbox prefilter → citywide bake 2.3 s (was a >5-min timeout). Tests `clip_trims_a_park_straddling_the_shore` + `clip_drops_a_park_entirely_in_water`. Re-baked both `parks.ospark` (M) + `parks_nyc.ospark` (all) + synced to web/dist. Verified native (island parks follow the land).
- [x] Fix the z-order layering of bridges -- as they are physically 'above' most other layers, they should not be clipped by the border boundaries line, for example. — root cause was a hidden *double-z* on the coastline outline: `stroke_ring_mesh` bakes its `z` arg (0.15) into the vertices **and** the spawn transform adds another 0.15, so the outline's effective z is **0.30**, not 0.15 (ACE ribbons similarly double to ~0.4). The flat bridge deck sits at exactly `BRIDGE_FLAT_Z` (mesh verts are z=0), so the earlier 0.16/0.185 were both *below* the outline's true 0.30 and the boundary stroke drew over every span. Raised `BRIDGE_FLAT_Z` to **0.5** — clearly above the outline (0.30) and ACE (0.4) so a crossing reads continuously over the water — while staying below the camera icons (1.0) and mobile agents (≤2.7) so vehicles still drive *over* the deck. Verified native (Brooklyn/Manhattan-bridge span shot: the coastline no longer cuts either deck).
- [x] Add an on-map box in the manhattan-only route centered in Brooklyn/Queens area, mentioning that the initial view is Manhattan-only, for performance; click here for the full city-wide version of the app (powerful computer required.). — reworked from a fixed screen-corner callout into a **map-pinned nudge that only surfaces when you pan to the outer boroughs** (per follow-up: the act of panning out there *is* the prompt). New `citywide_nudge` egui pass (separate from `ui_panel`, no shared params): a camera query projects **one world anchor per outer borough** (`CITYWIDE_NUDGE_ANCHORS` — Brooklyn SE, Queens E, the Bronx N, Staten Island SW, all well off the island) to screen via `Camera::world_to_viewport`, and the box is drawn at the `fixed_pos` of the **first anchor that sits comfortably inside the visible map area** (left of the 316px panel; one at a time) — so all are off-screen and hidden in the default island view, and whichever borough you pan into frame is where the prompt appears. Eyebrow names that borough (terracotta), body "This view is Manhattan only — kept light for performance, so the outer boroughs are empty here.", click-link "Open the full five-borough map →" (`go_citywide()` → `location.search=city=nyc` on web; no-op native), italic "a powerful computer is recommended". Gated `!citywide`. Verified native (pan shots to all four boroughs each show the correctly-labelled box pinned in empty space; Manhattan-framed shots hide it).
- [x] In city-wide route, estimate the number of trips threshold that would be required to render the MAX number of real-time trips in the city, per minute. We did a similar analysis for the manhattan-only route; write findings to a doc. — wrote `docs/CITYWIDE_TRIPS.md`. Via Little's Law `λ_sat = MAX_VEHICLES / W` with W (mean dwell ≈16.9 min) backed out of the measured Manhattan figures: **≈236 trips/min (~4/s)** saturate the on-screen cap (`MAX_VEHICLES=4000`, `agents.rs:30`). Threshold is identical in both builds (depends only on cap + dwell); what differs is the real arrival rate — Manhattan peaks at ~96% (never saturates), citywide's day-avg ~272/min already exceeds it (saturated ~16–18 h/day). Compounded with the bake-time top-5000 subsample, ~31% of real concurrent trips render at the evening peak. File:line map + the `taxi_peak` command to replace estimates with measured citywide numbers included; every assumption flagged.
- [x] Make the yellow color used for 'Simulated Day' and the number of cameras headline stat have more contrast against the white background. It should be a  more readable color. — the `TERRACOTTA` headline accent was literally `pal::YELLOW` (0xfacc15 → ~1.3:1 on the white paper plate / hero card). Swapped it to a deep terracotta/burnt-orange (0xc2410c, ~6:1) and pointed the three big-number headlines (`~N cameras`, analytical + live `~N devices`) at it too. Confidence-tier + heat-ramp yellow kept (those sit on ink). Verified native: "SIMULATED DAY" eyebrow now reads clearly.
- [x] Clicking 'Neighborhoods' button while it is already active should disable the mode. — the mode selector now captures `in_neigh` + the Neighborhoods chip's response; a re-click while active flips `params.mode` back to the default `Mode::Walkshed` (My area), mirroring the existing re-click-to-clear affordance on My area / A walk. Hover hint "Click again to leave Neighborhoods" added. Compiles; interactive toggle is real-browser-verifiable.
- [x] Tune the landmark labels to be more in the water / out of Brooklyn/Queens overlap, as they are presently. — `offshore_label_anchors` floated every label a fixed `LABEL_OFFSHORE_M` (420 m) past the nearest shore; east-shore labels point across the *narrow* East River, so the full reach landed their names on Brooklyn/Queens. Now east-pointing labels (`dir.x > 0`) float half the distance so they sit in the channel over the water, out of the far-borough fabric, while west-shore labels keep the full reach into the wide Hudson. Compiles; render-verified in the final citywide pass (labels sit over the East River, not Brooklyn/Queens).
- [x] Add on-hover animations to the further reading cards, and polish their design. We're going for an editorial, journalistic feel. — the further-reading cards (`.reading-card` in `web/index.html`) gained restrained editorial hover motion: a 3px lift + soft ink-tinted shadow (~180 ms ease-out), the teaser image now sits in a clipped `.rc-frame` and slow-zooms to 1.05 (450 ms) with a slight saturation bump, the title warms to the terracotta link color, and the kicker/badge widens tracking + warms on hover. Added `:focus-visible` outline for keyboard a11y and a `prefers-reduced-motion` block that disables all of it. Synced identically into `web/dist/index.html` (pure HTML/CSS/JS — no WASM rebuild). Card height unchanged, so the masthead strip + narrow-screen stack are untouched.
- [x] BIG FEATURE/ACCURACY REQUIREMENT: camera deduplication. We need to design an algorithm that deduplicates cameras reported by multiple different layer sources. I.e., some of the DOT cameras are also reported by DeFlock. — designed + documented the cross-source dedup in `docs/CAMERA_DEDUP.md`. The algorithm (`sim_core::group_sensors`) already existed: union-find proximity clustering (grid-bucketed, ~O(n) over ~31k cams) that merges **only across sources** within `FIXED_GROUP_RADIUS_M`=15 m, assigning each physical camera a `group` id + a `confirmed` flag (any surveyed member). The A→B path already deduped via `ExposureTally.fixed_groups`, but **the My-area (walkshed) path was keying its `seen` set on the raw per-layer `s.id`** — so a DOT cam + its DeFlock ALPR twin counted twice. Fixed `walkshed_exposure` to key on `s.group` (one physical camera = one count) with group-aware recall (confirmed groups at face value, CCTV-census-only groups keep the recall inflation), mirroring `grouped_fixed_devices` so both modes agree. Map markers stay per-source on purpose (attestation is the point); only the counts collapse. Tests: `walkshed_dedups_colocated_cross_source_camera` + `walkshed_recall_inflates_only_unconfirmed_groups`. Workspace green (107 tests).
- [x] the ON MAP labels for ALPR/Flock cameras should say 'ALPR', not 'Flock'. — the on-map wordmark marker was the texture `brand_flock.png`, baked by `generate_icons.py` as the literal text "FLOCK". Renamed the asset to `brand_alpr.png` rendering "ALPR" (category, not brand — matches the Operators-tower header from items 14/38), repointed `main.rs` (the ALPR marker + Operators chip share that one texture handle), regenerated via PIL, removed the stale `brand_flock.png` from assets + web/dist. Verified the new texture reads "ALPR".
- [x] Remove the SOHO starter focus on app startup (currently we show the 'My Area' mode for SoHo Manhattan, we should disable that and assume no mode at startup.) — added a `Mode::None` startup state (the new `Params::default().mode`), deleted the `seed_example_walkshed` system entirely (no SoHo example), and handled `None` across every `match params.mode` (description line, address inputs, the hero card → new `result_none` "Start exploring" prompt, click handler = inert, sync/reset status = empty). The three mode chips render unselected at startup; clicking one enters that mode. Verified native (`ourspace_map.png` shows "Start exploring", no mode selected).
- [x] In Neighborhoods mode, the name of the neighborhood / borough on hover in the right side panel should not be the dark red we use for surveillance stats -- just use a black color. — `result_neighborhoods` now paints the "{name} · {borough}" headline in `pal::ZINC_100` (darkest ink) instead of `TERRACOTTA`; the terracotta stays reserved for surveillance numbers.
- [x] Remove the 'How Watched is a place in Manhattan?' tagline from the top of the right sidebar. — dropped the masthead `RichText` line; the panel now leads straight into the mode selector (the page header already carries the framing). Verified in `ourspace_map.png`.
- [x] Make the time-of-day scrubber panel movable within the map viewport -- sometimes it currently occludes parts of the map, and so it should be movable. Ensure you write a proper architectural pattern for movable components, don't just add a hacky fix. — wrote a reusable `movable.rs` module: a `MovablePanels` resource (positions keyed by stable id) + a `movable_panel(ctx, store, id, width, seed, frame, body)` helper that seeds from a default screen anchor, draws a **drag grip** (the only drag surface, so inner widgets like the scrub bar keep their own pointer handling), and clamps to the viewport (re-clamps on resize). Until the user drags, the panel tracks its live default each frame (so a first-frame placeholder screen can't freeze it off-screen — the bug found + fixed via instrumentation). The time bar adopts it (seeds bottom-center); future overlays can opt in. Verified native: grip dots render, panel sits bottom-center, draggable.
- [x] Make the yellow used for '+ XX mobile cameras here now' and also in the on-map neighborhood num. mobile cameras label (when we toggle on the camera-density heatmap) a heavier font weight, so its more readable. Make sure contrast and readability is sufficient. — two surfaces. Panel ("+ N mobile sensors here now"): the bright `YELLOW` was ~1.4:1 on the light card, so it now uses a new `pal::GOLD` token (0xa16207, ~4.3:1, still yellow-family) + the ExtraBold display face. On-map ("N mobile" label, heatmap on): kept the bright hazard yellow (good contrast on its dark plate) but loaded `HostGrotesk-ExtraBold` as a Bevy `Font` for the span (real heavier weight) + bumped its size.
- [x] Remove the 'include all five boroughs' checkbox on the right sidebar -- its now superseded by the on-map outer-borough trigger modals. — dropped the `ui.checkbox(&mut params.neighborhoods_all, …)` from the Neighborhoods address slot. The `neighborhoods_all` field stays (still driven by the build scope: citywide → true, Manhattan → false), only the user toggle is gone — scope is reached via the on-map outer-borough nudge → the `?city=nyc` build.
- [x] Add icons above each of the three modes on the right sidebar -- use a walking person icon for the a->b walk, use a circle with a pin inside it for the 'my area', and use the shapefile outline of the    'Financial District' neighborhood for the 'Neighborhoods' mode. Size everything appropriately, keep readable and polished, and don't color the icons. — the selector is now three icon chips (a new `mode_button` helper): each column is one click target with a monochrome **ink** icon over its label. Painters: `paint_pin_in_circle` (a ring + a centered location pin), `paint_walker` (a mid-stride stick figure), and `paint_neighborhood_outline` (the **real Financial District ring** from `sim.neighborhoods`, fit to the box with Y flipped from ENU; falls back to a rounded square before the asset loads). Re-click affordances preserved (My area/A→B clear placed visuals; Neighborhoods leaves to `None`). Verified native (`ourspace_map.png`).
- [x] *Big Addition.* We're adding a new 'Institutions' mode, where we sort different NYC institution types by the surveillance they experiment. Pull in the NYC facilities database, which has an extremely rich metadata we can utilize. https://data.cityofnewyork.us/City-Government/Facilities-Database/ji82-xba5. Let's start with schools and libraries. This should be a new entry under 'Explore' on the right sidebar. Include support for marking the institutions on the map when their class is visible, flying to an institution when its corresponding ranking entry is clicked, etc. The ranking menu should appear (with polished transition) on a new LEFT sidebar -- make sure you do not occlude the 'Simulated Day' header when you develop. — full end-to-end. **Data:** new `bake-facilities` (`data-pipeline/facilities.rs`) bakes the NYC Facilities DB (ji82-xba5, pre-filtered to `SCHOOLS (K-12)` + `LIBRARIES`) into a `FacilityLayer` (`sim_core::assets`), borough-filterable → `facilities.osfac` (679 Manhattan) + `facilities_nyc.osfac` (3359 all-borough); snapshot fetch + bake wired into `tools/bake_citywide.sh`. **Loader:** `postcard_asset!(FacilitiesRes, FacilityLayer)` on `.osfac`, `LoadingHandles.facilities` (Manhattan vs citywide path). **Scoring:** each institution is scored by **deduplicated** fixed cameras within `FACILITY_SCAN_M`=200 m (one per physical-camera `group`, reusing `Sim.cam_index` + the walkshed/headline dedup), built in `build_world` into a `FacilityDirectory` (+ surveillance-ranked index via the unit-tested `rank_facilities`). **Explore toggle:** "Institutions (schools & libraries)" under EXPLORE (mutually exclusive with the operators/coverage takeovers). **Left sidebar:** new `institutions_panel` egui pass — a card that **slides in from the left** (smoothstep on `InstitutionsView.t`, driven by `institutions_tick`), anchored at y=92 so it never occludes the "SIMULATED DAY" header; shows per-type averages (the type ranking: Schools 11.2 / Libraries 11.1 cams avg), Schools/Libraries filter chips, and a scrollable ranked list (gold camera-count badge + name + kind dot). **Map:** per-class merged-quad markers (indigo school / teal library, cool = subject not sensor) gated on the view + filters; a reusable highlight ring follows the selection. **Interactions:** clicking a list row *or* a map marker selects it → highlights + flies the camera there (`CameraFly`/`FlyTo::Point`); map clicks are facility-only while the view is up. Tests: `facilities::bakes_classifies_and_filters_by_borough`, `facility_tests::ranks_by_camera_count_then_name`. Verified native (`ourspace_institutions.png`): panel docked below the header, markers across Manhattan, ranking populated, EXPLORE toggle active.
- [x] Make the morph between text label and shape in the 'Stack sensors by operator' view transtion more seamless and subtle -- right now, the labels and clearly swapped out for the shapes in the middle of the flow animation. — item 44 moved the texture→solid swap to the flight midpoint, but it was still a hard pop (glyph → filled square in one frame). Now a **cross-dissolve** masks it: `morph_dissolve(t)` dips the chip's alpha to `MORPH_DISSOLVE_FLOOR` (0.22) across a window (`±MORPH_DISSOLVE_HALF` 0.14) centred on `CHIP_MORPH_T`, so the mark briefly fades out *as* the texture is dropped and fades back in as the solid chip — the discrete shape change happens while the chip is dimmest and reads as a soft dissolve, not a swap. Both `operators_chip_material` (fixed, incl. ALPR maker bands) and `operators_mobile_material` (peds/buses) drive it off progress each frame within the window (still edge-cheap outside it); `ColorMaterial` already defaults to `AlphaMode2d::Blend` so the dim shows on both the textured and solid sides. The exact frame-by-frame smoothness is a motion-quality judgment best confirmed in a real browser.